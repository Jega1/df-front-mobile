import 'package:flutter/material.dart';

class TrainingComplete extends StatefulWidget {
  @override
  _TrainingCompleteState createState() => _TrainingCompleteState();
}

class _TrainingCompleteState extends State<TrainingComplete> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("completed training")),
    );
  }
}
